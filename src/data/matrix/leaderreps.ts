// LeaderReps — TODO: fill in from leaderreps codespace.
// Each row key matches _rows.ts. Leave as null until you have a real answer.
import type { CompanyData } from './_rows';

const data: CompanyData = {
  // Identity
  'id.product':         'LeaderReps',
  'id.legal':           'leaderreps LLC',
  'id.domain':          'www.leaderreps.com',
  // Leave the rest unset — fill in from the LeaderReps codespace.
};

export default data;
