---
const path = Astro.url.pathname;
const isActive = (p: string) => path === p || (p !== '/' && path.startsWith(p));

const links = [
  { href: '/portfolio', label: 'Portfolio' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
];
---
<header class="border-b border-[var(--color-rule)]">
  <div class="mx-auto max-w-6xl px-6 h-20 flex items-center justify-between">
    <a href="/" class="flex items-center gap-3 group">
      <span class="font-display text-3xl sm:text-4xl tracking-tight leading-none">Sage<span class="text-[var(--color-accent)]">CG</span></span>
      <span class="hidden sm:inline font-mono text-[10px] uppercase tracking-[0.18em] text-[var(--color-muted)] border border-[var(--color-rule)] px-1.5 py-0.5 rounded-sm">
        Venture Studio
      </span>
    </a>
    <nav class="flex items-center gap-1 sm:gap-2 text-sm">
      {links.map((l) => (
        <a
          href={l.href}
          class:list={[
            'px-3 py-2 rounded-md transition-colors',
            isActive(l.href)
              ? 'text-[var(--color-ink)] bg-[var(--color-rule)]/60'
              : 'text-[var(--color-muted)] hover:text-[var(--color-ink)]',
          ]}
        >
          {l.label}
        </a>
      ))}
    </nav>
  </div>
</header>
